$(document).ready(function() {

	$('.owl-carousel').owlCarousel({
    margin:0,
    loop:true,
    //dots:true,
    autoplay:true,
    autoplayTimeout:3000,
    items:1,
    //nav: true
	});
});